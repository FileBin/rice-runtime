name: test
version: 0.0.1

type:
  csharp:
    runners:
      mono: ">=4.0.0"

dependencies:
  packages:
    - angus: ">=0.1.0"
  native:
    - glib

entrypoints:
  - main

resources:
  preloaded: '*'
  hasExternal: true